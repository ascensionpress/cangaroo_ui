window.cangaroo.updateFlashElements(
  "<%=j Cangaroo::FlashHelper.to_bootstrap_alert(self, flash) %>"
);
window.cangaroo.replaceElementById(
  "js-transaction-table-row-<%= @tx.id %>",
  "<%=j render partial: 'cangaroo/transactions/transaction_row', locals: { transaction: @tx } %>"
);
