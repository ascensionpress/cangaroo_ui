window.cangaroo.updateFlashElements(
  "<%=j CangarooUI::FlashHelper.to_bootstrap_alert(self, flash) %>"
);
window.cangaroo.replaceElementById(
  "js-transaction-table-row-<%= @tx.id %>",
  "<%=j render partial: 'cangaroo_ui/transactions/transaction_row', locals: { transaction: @tx } %>"
);

window.cangaroo.replaceElementById(
  "js-error-notice",
  "<%=j render partial: 'cangaroo_ui/error_notice' %>"
);
